<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme that uses DayNight for automatic light/dark switching -->
    <style name="Theme.OverseerrClient" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    
    <!-- Splash screen theme using the SplashScreen library -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.OverseerrClient</item>
    </style>
</resources>
